{
  "name": "idle-sync",
  "version": "2.1.0",
  "private": true,
  "description": "IDLE SYNC: telecomando AI vocale per il Mac con Claude Code, sincronizzato tra iPhone, iPad e browser",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && for f in lib/*.js lib/engines/*.js public/js/*.js; do node --check $f || exit 1; done"
  },
  "engines": {
    "node": ">=20.12"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.128.0",
    "@modelcontextprotocol/sdk": "^1.30.1",
    "qrcode": "^1.5.4",
    "ws": "^8.18.0"
  }
}
